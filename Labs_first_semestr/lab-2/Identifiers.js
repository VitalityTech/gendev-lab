'use strict'

// Проста змінна
let myName = "Vitaliy";

// Константа
const birthYear = 2007;

// Функція яка використовує цю змінну
const say_hello = (name) => {
    console.log(`Hello, ${name}!`);
}

say_hello(myName);